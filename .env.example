# Copy to .env in Dokploy (Environment tab) or locally. Never commit the real .env.
# The database is a SEPARATE Dokploy MySQL service; DB_HOST is its appName on
# the dokploy-network overlay (see the Dokploy MySQL service page).
DB_HOST=mysql-override-1080p-protocol-6t6chk
DB_NAME=amu
DB_USER=amu
DB_PASSWORD=change-me-strong
WP_URL=https://animemangaupdates.com

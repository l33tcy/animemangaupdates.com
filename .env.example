# Copy to .env in Dokploy (Environment tab) or locally. Never commit the real .env.
DB_NAME=amu
DB_USER=amu
DB_PASSWORD=change-me-strong
DB_ROOT_PASSWORD=change-me-strong-root
# DB_HOST defaults to the bundled `db` service. Point elsewhere for an external DB.
DB_HOST=db
WP_URL=https://animemangaupdates.com
